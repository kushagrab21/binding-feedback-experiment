{
  "task_id": "task_k1_021",
  "seed_name": "seed_021",
  "tier": 1,
  "n_bugs": 2,
  "bug_types": [
    "missing-edge-case",
    "off-by-one"
  ],
  "constituent_mutations": [
    {
      "bug_type": "off-by-one",
      "note": "After a sign, the digit start index is one too high, dropping the first digit (and raising on single-digit signed input)."
    },
    {
      "bug_type": "missing-edge-case",
      "note": "The no-digits-after-sign guard is dropped, so a lone '+'/'-' returns 0 instead of raising."
    }
  ],
  "function_name": "parse_signed_int",
  "description": "Parse a signed integer string without int() on the whole string.",
  "difficulty": null
}
