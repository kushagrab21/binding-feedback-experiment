{
  "task_id": "task_k2_046",
  "seed_name": "seed_003",
  "tier": 2,
  "n_bugs": 3,
  "bug_types": [
    "off-by-one",
    "wrong-comparison",
    "wrong-operator"
  ],
  "constituent_mutations": [
    {
      "bug_type": "off-by-one",
      "note": "Range upper bound loses +1, so n itself is never counted as a divisor."
    },
    {
      "bug_type": "wrong-operator",
      "note": "Counter is decremented instead of incremented, so the divisor count is negated."
    },
    {
      "bug_type": "wrong-comparison",
      "note": "== becomes !=, so non-divisors are counted instead of divisors."
    }
  ],
  "function_name": "count_divisors",
  "description": "Count the positive divisors of a positive integer.",
  "difficulty": null
}
