{
  "task_id": "task_k2_050",
  "seed_name": "seed_019",
  "tier": 2,
  "n_bugs": 3,
  "bug_types": [
    "inverted-condition",
    "wrong-comparison",
    "wrong-operator"
  ],
  "constituent_mutations": [
    {
      "bug_type": "wrong-comparison",
      "note": "<= becomes <, so a one-element search window is skipped and single elements are missed."
    },
    {
      "bug_type": "wrong-operator",
      "note": "Midpoint uses * instead of //, driving mid out of range so the lookup raises IndexError. (A low-high subtraction was rejected: it produces a fixed-point infinite loop rather than a killable mutant.)"
    },
    {
      "bug_type": "inverted-condition",
      "note": "The half-selection test is negated, so the search moves the wrong direction and misses targets."
    }
  ],
  "function_name": "binary_search",
  "description": "Binary search for a target in a sorted list.",
  "difficulty": null
}
